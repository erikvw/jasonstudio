# Required in live.py (production). Optional in debug.py (has defaults).
DJANGO_SECRET_KEY=change-me-to-a-random-50-char-string
DJANGO_ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# Database (live.py only) — pick one:
DATABASE_URL=postgres://jasonstudio:change-me@127.0.0.1:5432/jasonstudio
# DATABASE_URL=mysql://jasonstudio:change-me@127.0.0.1:3306/jasonstudio
